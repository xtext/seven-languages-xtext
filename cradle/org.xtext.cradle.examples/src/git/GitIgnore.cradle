package git

task SetGitignore {
	for(proj: git.containedJavaProjects) {
		for(dir: newArrayList( proj:/src-gen:, proj:/xtend-gen: )) {
			if(dir.exists) {
				println("processing " + dir)
//				dir:/.gitignore:.deleteDirectoryContents
//				dir:/.gitignore:.setFileContents("*\n!.gitignore\n")
			}
		}
	}
}